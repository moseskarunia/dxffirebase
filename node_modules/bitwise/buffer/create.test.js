"use strict";

var _ = _interopRequireDefault(require("../"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

test('with less than one byte', function () {
  var buffer = _.default.buffer.create(_.default.string.toBits('10011'));

  expect(_.default.buffer.read(buffer).join()).toBe(_.default.string.toBits('1001 1000').join());
});
test('with one byte', function () {
  var buffer = _.default.buffer.create(_.default.string.toBits('0111 1100'));

  expect(_.default.buffer.read(buffer).join()).toBe(_.default.string.toBits('0111 1100').join());
});
test('with more than one byte', function () {
  var buffer = _.default.buffer.create(_.default.string.toBits('1111 0001 1010'));

  expect(_.default.buffer.read(buffer).join()).toBe(_.default.string.toBits('1111 0001 1010 0000').join());
});
test('with multiple bytes', function () {
  var buffer = _.default.buffer.create(_.default.string.toBits('10101101 11100101 01100011'));

  expect(_.default.buffer.read(buffer).join()).toBe(_.default.string.toBits('10101101 11100101 01100011').join());
});