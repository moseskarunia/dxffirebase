"use strict";

var _xor = _interopRequireDefault(require("./xor"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

test('XOR', function () {
  var bits1 = [1, 0, 0, 0, 1, 1, 0, 1];
  var bits2 = [0, 1, 1, 0, 0, 1, 0, 0];
  var expected = [1, 1, 1, 0, 1, 0, 0, 1];
  expect((0, _xor.default)(bits1, bits2)).toEqual(expected);
});