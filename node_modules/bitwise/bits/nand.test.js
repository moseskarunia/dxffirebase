"use strict";

var _nand = _interopRequireDefault(require("./nand"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

test('NAND', function () {
  var bits1 = [1, 0, 0, 0, 1, 1, 0, 1];
  var bits2 = [0, 1, 1, 0, 0, 1, 0, 0];
  var expected = [1, 1, 1, 1, 1, 0, 1, 1];
  expect((0, _nand.default)(bits1, bits2)).toEqual(expected);
});