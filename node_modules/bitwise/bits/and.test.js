"use strict";

var _and = _interopRequireDefault(require("./and"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

test('AND', function () {
  var bits1 = [1, 0, 0, 0, 1, 1, 0, 1];
  var bits2 = [0, 1, 1, 0, 0, 1, 0, 0];
  var expected = [0, 0, 0, 0, 0, 1, 0, 0];
  expect((0, _and.default)(bits1, bits2)).toEqual(expected);
});